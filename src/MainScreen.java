import javax.swing.*;

public class MainScreen extends JFrame {
    MainScreen(){
        setDefaultCloseOperation(EXIT_ON_CLOSE);
        setSize(700, 500);
        setTitle("SDMS");
        setVisible(true);
    }
}
